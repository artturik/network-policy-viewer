/// <reference types="react" />
import { EdgeProps } from "../../../react-flow";
export default function DefaultEdge({ id, sourceX, sourceY, targetX, targetY, sourcePosition, targetPosition, style, data, markerEndId }: EdgeProps): JSX.Element;
